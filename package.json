{
  "type": "module",
  "scripts": {
    "build": "npm run build:version",
    "build:version": "node scripts/generate-build-version.mjs",
    "check:image-serving": "node scripts/check-image-serving.mjs",
    "postinstall": "npm run build:version",
    "deploy:background": "wrangler deploy -c wrangler.background.toml"
  },
  "devDependencies": {
    "wrangler": "latest"
  },
  "dependencies": {
    "@jsquash/jpeg": "^1.6.0",
    "@jsquash/png": "^3.1.1",
    "@jsquash/webp": "^1.5.0"
  }
}
