{
  "type": "module",
  "scripts": {
    "build": "npm run build:version",
    "build:version": "node scripts/generate-build-version.mjs",
    "check:image-serving": "node scripts/check-image-serving.mjs",
    "check:guest-image-visibility": "node scripts/check-guest-image-visibility.mjs",
    "check:novelai-v5": "node scripts/check-novelai-v5.mjs",
    "postinstall": "npm run build:version",
    "deploy:background": "wrangler deploy -c wrangler.background.toml"
  },
  "devDependencies": {
    "wrangler": "latest"
  },
  "dependencies": {
    "@jsquash/jpeg": "^1.6.0",
    "@jsquash/png": "^3.1.1",
    "@jsquash/webp": "^1.5.0"
  }
}
